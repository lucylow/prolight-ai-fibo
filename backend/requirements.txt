# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Pydantic for settings and validation
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP client
httpx==0.25.1

# Retry logic
tenacity==8.2.3

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
respx==0.20.2

# Code quality
black==23.11.0
flake8==6.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL adapter
alembic==1.13.0  # Database migrations

# Utilities
python-dotenv==1.0.0

# Payment processing
stripe==7.8.0
python-jose[cryptography]==3.3.0

# Email
aiosmtplib==3.0.1
email-validator==2.1.0

# AWS S3
boto3==1.34.0

# HTTP requests (for reCAPTCHA and Greenhouse)
requests==2.31.0

# Image processing for palette extraction
Pillow==10.1.0
numpy==1.26.2

# Redis for vector store and conversation memory
redis[hiredis]==5.0.1

# Celery for async task processing
celery[redis]==5.3.4

# Server-Sent Events for streaming
sse-starlette==1.8.2

# Schema generation (optional - for generating Pydantic models from FIBO JSON schema)
datamodel-code-generator>=0.23.0

# Composition analysis (optional - CLIP for advanced saliency)
# Uncomment the following lines to enable CLIP-based saliency analysis:
# torch>=2.0.0
# torchvision>=0.15.0
# ftfy
# regex
# tqdm
# git+https://github.com/openai/CLIP.git

# FIBO Model Support (Bria AI)
# Install diffusers from source for FIBO support:
# pip install git+https://github.com/huggingface/diffusers
torch>=2.0.0
torchvision>=0.15.0
# diffusers>=0.30.0  # Install from source: git+https://github.com/huggingface/diffusers
google-genai>=0.2.0  # For Gemini API integration
boltons>=23.0.0
ujson>=5.9.0
sentencepiece>=0.1.99
accelerate>=0.25.0
transformers>=4.36.0

# Anthropic SDK for MCP (Model Context Protocol)
anthropic>=0.34.0
